int width;
int height;
int row = rand() % 1;
int col = rand() % 1;

stuct position {
	int row;
	int col;
}

typedef struct {
	int dots;
	struct p = position(row, column);
} square;

int density(square s) {
	int density;
	int dots = s.dots;
	int area = s.area;
	density = dots/area;
	return density;
}

int main() {
	int * grid = malloc(10 * sizeof(int *));
	int square = *((grid + row) + col)
	// area of a single square in the grid
	int area = width * height;
	// dot density in a single square
	int density = square.dots/square.area;

	square1 = square(dots, position);
	square2 = square(dots, position);
	// dd = density difference
	int dd = square1.
	
	
}