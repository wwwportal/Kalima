# Change the app name to Diameme Manager and update all references
DiamemeManager

App updateTitle() {
	char* title = "Diameme Manager";
	char** titleReferences = app.getTitleReferences();
	app.setTitle(title);
	for (titleReference in titleReferences) { 
		titleReference.update("Diameme Manager");
	}
}
////////////////////////////
Char* format = "[i1] <-(i2/i1)- -(i1/i2)-> [i2]";

void defineRelationship(char* i1, char* i2) {
    
}

int main() {


}
Use format 
to describe the relationship between the scanner and the transition table, defining A as the relation from e2